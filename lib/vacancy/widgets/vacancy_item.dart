import 'package:flutter/cupertino.dart';

class VacancyItem extends StatelessWidget {
  const VacancyItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
